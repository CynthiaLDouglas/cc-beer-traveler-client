  <section>
    <h3>{{post.title}}</h3>
    <h4>{{post.placeName}}</h4>
    <h5>{{post.cityLoc}}{{post.stateLoc}}</h5>
    <p>{{post.content}}</p>
    <p>{{post._id}}</p>
    <button class="btn delete"data-toggle="collapse" data-target=".delete-confirm"> Delete this post </button>
    <button class="btn update" data-toggle="collapse" data-target=".update-form">Update this Post</button>
  </section>
  <div aria-expanded="false" class="collapse update-form">
    <form data-id="{{post._id}}" class="update-one-post">Update Blog Post<br />
      <input name="post[title]" type="text" placeholder="Enter your title here"><br />
      <input name="post[placeName]" type="text" placeholder="Enter name of establishment here"><br />
      <input name="post[cityLoc]" type="text" placeholder="Enter city here"><br />
      <input name="post[stateLoc]" type="text" placeholder="Enter state here"><br />
      <input name="post[content]" type="text" placeholder="Enter content here"><br />
      <button class="btn" type="submit">Update Post</button>
    </form>
  </div>
  <div aria-expanded="false" class="collapse delete-confirm">Are you sure?
    <button class="btn delete-post" type="submit" data-id="{{post._id}}">Delete Post</button>
  </div>
